@layout('common/noauth_layouts')

@section('content')
<div class="flex items-center justify-center grow bg-center bg-no-repeat">
  <div class="card max-w-[500px] w-full">
    <div class="card-body flex flex-col gap-5 p-10 text-center">
      <div class="flex justify-center">
        <i class="ki-filled ki-setting-4 text-6xl text-gray-400"></i>
      </div>
      <h2 class="text-2xl font-semibold text-gray-900">系统维护中</h2>
      <p class="text-gray-600">{{ message || '系统正在维护，请稍后再试。' }}</p>
      <div class="flex justify-center gap-2.5">
        <button type="button" class="btn btn-primary" onclick="window.location.reload()">
          刷新页面
        </button>
      </div>
    </div>
  </div>
</div>
@endsection

